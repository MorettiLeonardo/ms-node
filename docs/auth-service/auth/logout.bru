meta {
  name: Logout
  type: http
  seq: 5
}

post {
  url: {{auth_service_url}}/logout
  body: json
  auth: bearer
}

auth:bearer {
  token: {{access_token}}
}

body:json {
  {
    "refresh_token": "{{refresh_token}}"
  }
}
